# 🚀 Подробная инструкция по деплою на Render.com

## 📋 Подготовка перед деплоем

### Шаг 0: Проверка файлов

Убедитесь, что у вас есть следующие файлы в корне проекта:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `Procfile` - команда запуска
- ✅ `requirements.txt` - зависимости Python
- ✅ `gunicorn_config.py` - конфигурация Gunicorn

Все эти файлы уже созданы! ✅

---

## 🔐 Шаг 1: Регистрация на Render.com

1. Откройте браузер и перейдите на [https://render.com](https://render.com)

2. Нажмите кнопку **"Get Started for Free"** или **"Sign Up"**

3. Выберите способ регистрации:
   - **GitHub** (рекомендуется) - используйте свой GitHub аккаунт
   - **Email** - зарегистрируйтесь через email

4. Заполните форму регистрации:
   - Email
   - Пароль
   - Подтвердите email (проверьте почту)

5. После регистрации вы попадете на Dashboard (панель управления)

---

## 📦 Шаг 2: Подготовка GitHub репозитория

### 2.1. Создание репозитория на GitHub (если еще нет)

1. Откройте [https://github.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name**: `school_21_bot_2` (или любое другое имя)
   - **Description**: "Telegram bot for room booking"
   - **Visibility**: Public или Private (на ваш выбор)
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore" (у вас уже есть)
4. Нажмите **"Create repository"**

### 2.2. Загрузка проекта в GitHub

**Вариант A: Через Git командную строку (рекомендуется)**

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git (если еще не инициализирован)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: School21 Bot"

# Добавление удаленного репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/school_21_bot_2.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

**Вариант B: Через GitHub Desktop**

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в свой аккаунт
3. File → Add Local Repository
4. Выберите папку проекта
5. Нажмите "Publish repository"
6. Выберите имя и нажмите "Publish"

**Вариант C: Через веб-интерфейс GitHub**

1. На странице созданного репозитория нажмите **"uploading an existing file"**
2. Перетащите все файлы проекта в браузер
3. Нажмите **"Commit changes"**

---

## 🔗 Шаг 3: Подключение GitHub к Render

1. В Dashboard Render.com нажмите **"New +"** → **"Web Service"**

2. Вы увидите окно подключения:
   - Если вы зарегистрировались через GitHub, выберите **"Connect account"** или **"Configure account"**
   - Если через email, нажмите **"Connect GitHub"** или **"Connect account"**

3. Разрешите Render доступ к вашим репозиториям:
   - Выберите **"All repositories"** (рекомендуется) или **"Only select repositories"**
   - Нажмите **"Install"** или **"Authorize"**

4. После подключения выберите ваш репозиторий `school_21_bot_2` из списка

---

## ⚙️ Шаг 4: Создание Web Service на Render

После выбора репозитория Render покажет форму настройки:

### 4.1. Основные настройки:

**Name**: `school21-bot` (или любое другое имя)

**Region**: Выберите ближайший регион:
- **Oregon (US West)** - для США
- **Frankfurt (EU)** - для Европы
- **Singapore (Asia)** - для Азии

**Branch**: `main` (или `master`, если используете master)

**Root Directory**: Оставьте пустым (проект в корне)

**Runtime**: `Python 3` (Render определит автоматически)

**Build Command**: Render автоматически использует `render.yaml`, но можно указать вручную:
```
pip install -r requirements.txt
```

**Start Command**: Render автоматически использует `Procfile`, но можно указать вручную:
```
cd backend && gunicorn -c gunicorn_config.py app:app
```

### 4.2. План (Plan):

Выберите **"Free"** (бесплатный план)

### 4.3. Переменные окружения (Environment Variables):

Нажмите **"Add Environment Variable"** и добавьте следующие переменные:

#### Переменная 1: TELEGRAM_BOT_TOKEN

**Ключ**: `TELEGRAM_BOT_TOKEN`  
**Значение**: Ваш токен бота от [@BotFather](https://t.me/BotFather)

**Как получить токен:**
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot` или `/mybots`
4. Если бот уже создан, выберите его и нажмите **"API Token"**
5. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### Переменная 2: BACKEND_URL

**Ключ**: `BACKEND_URL`  
**Значение**: Пока оставьте пустым или временный URL. После деплоя вернетесь и обновите на:
```
https://school21-bot.onrender.com
```
(Замените `school21-bot` на имя вашего сервиса)

**Важно:** После успешного деплоя вернитесь в настройки и обновите `BACKEND_URL` на ваш реальный Render URL.

#### Дополнительные переменные (опционально):

Если нужно, добавьте:
- `PORT` = `10000` (Render автоматически установит, но можно указать явно)
- `FLASK_DEBUG` = `False` (для продакшена)

### 4.4. Нажмите **"Create Web Service"**

---

## ⏳ Шаг 5: Ожидание деплоя

1. Render начнет процесс деплоя:
   - **Building** - установка зависимостей
   - **Deploying** - запуск приложения
   - **Live** - приложение работает!

2. Это займет **3-5 минут**

3. Вы увидите логи в реальном времени:
   - Установка зависимостей
   - Запуск Gunicorn
   - Ошибки (если есть)

4. После успешного деплоя вы получите URL:
   - Формат: `https://school21-bot.onrender.com`
   - Этот URL будет вашим адресом бота

---

## 🔍 Шаг 6: Проверка работы

1. Откройте URL вашего приложения в браузере
2. Должна открыться главная страница бота
3. Проверьте API: `https://your-app.onrender.com/api/ping`
   - Должен вернуть: `{"status": "ok", "message": "Backend живой!"}`

---

## 🤖 Шаг 7: Настройка Telegram бота

### 7.1. Обновление BACKEND_URL в Render

После успешного деплоя вернитесь в настройки вашего Web Service в Render:

1. Откройте ваш Web Service в Render Dashboard
2. Перейдите на вкладку **"Environment"**
3. Найдите переменную `BACKEND_URL`
4. Обновите значение на ваш Render URL:
   ```
   https://school21-bot.onrender.com
   ```
   (Замените `school21-bot` на имя вашего сервиса)
5. Нажмите **"Save Changes"**
6. Render автоматически перезапустит приложение

### 7.2. Запуск бота локально

**Вариант A: Через .env файл (рекомендуется)**

1. Создайте файл `.env` в корне проекта (если его еще нет)

2. Добавьте в `.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
BACKEND_URL=https://school21-bot.onrender.com
```

3. Запустите бота:
```bash
python bot.py
```

**Вариант B: Через переменные окружения системы**

```bash
# Windows (PowerShell)
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
$env:BACKEND_URL="https://school21-bot.onrender.com"
python bot.py

# Windows (CMD)
set TELEGRAM_BOT_TOKEN=ваш_токен
set BACKEND_URL=https://school21-bot.onrender.com
python bot.py

# Linux/Mac
export TELEGRAM_BOT_TOKEN="ваш_токен"
export BACKEND_URL="https://school21-bot.onrender.com"
python bot.py
```

### 7.3. Альтернатива: Запуск бота на Render (Background Worker)

Если не хотите запускать бота локально, можно создать Background Worker на Render:

1. В Render Dashboard нажмите **"New +"** → **"Background Worker"**
2. Выберите тот же репозиторий
3. Настройки:
   - **Name**: `school21-bot-worker`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
   - **Environment Variables**: 
     - `TELEGRAM_BOT_TOKEN` = ваш токен
     - `BACKEND_URL` = `https://school21-bot.onrender.com`
4. Нажмите **"Create Background Worker"**

**Важно:** 
- Background Worker на бесплатном плане тоже засыпает
- Для постоянной работы бота лучше запускать локально или на VPS

---

## 🔄 Шаг 8: Настройка Uptime Robot (предотвращение засыпания)

Render засыпает после 15 минут бездействия. Uptime Robot будет "будить" его.

### 8.1. Регистрация на Uptime Robot

1. Откройте [https://uptimerobot.com](https://uptimerobot.com)
2. Нажмите **"Sign Up"** (бесплатно)
3. Заполните форму:
   - Email
   - Пароль
   - Подтвердите email

### 8.2. Создание монитора

1. После входа нажмите **"Add New Monitor"**

2. Заполните форму:

   **Monitor Type**: `HTTP(s)`

   **Friendly Name**: `School21 Bot`

   **URL (or IP)**: Ваш Render URL:
   ```
   https://school21-bot.onrender.com/api/ping
   ```

   **Monitoring Interval**: `5 minutes` (бесплатный план)

   **Alert Contacts**: Выберите email для уведомлений

3. Нажмите **"Create Monitor"**

4. Готово! Теперь Uptime Robot будет проверять ваш бот каждые 5 минут, и Render не будет засыпать

---

## 📝 Шаг 9: Обновление кода (последующие деплои)

После изменения кода:

1. **Закоммитьте изменения в Git:**
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

2. **Render автоматически обнаружит изменения и начнет новый деплой**

3. Деплой займет 3-5 минут

---

## ⚠️ Решение проблем

### Проблема: "Build failed"

**Решение:**
- Проверьте логи в Render Dashboard
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что Python версия указана правильно

### Проблема: "Application error"

**Решение:**
- Откройте логи в Render Dashboard
- Проверьте, что `TELEGRAM_BOT_TOKEN` установлен правильно
- Убедитесь, что команда запуска правильная

### Проблема: "Database not found"

**Решение:**
- SQLite файл создается автоматически при первом запуске
- Убедитесь, что путь к БД правильный в `backend/database.py`

### Проблема: Приложение засыпает

**Решение:**
- Настройте Uptime Robot (см. Шаг 8)
- Или перейдите на платный план Render ($7/месяц)

---

## 📊 Мониторинг и логи

### Просмотр логов в Render:

1. Откройте ваш Web Service в Render Dashboard
2. Перейдите на вкладку **"Logs"**
3. Вы увидите логи в реальном времени

### Просмотр метрик:

1. В Dashboard Render перейдите на вкладку **"Metrics"**
2. Вы увидите:
   - CPU использование
   - Memory использование
   - Request count

---

## 🔐 Безопасность

### Важные моменты:

1. **НЕ коммитьте `.env` файлы в Git** (уже в `.gitignore`)
2. **НЕ публикуйте `TELEGRAM_BOT_TOKEN`** в коде
3. Используйте переменные окружения для всех секретов
4. Регулярно обновляйте зависимости

---

## 💰 Стоимость

**Бесплатный план Render:**
- ✅ $0/месяц
- ✅ 750 часов работы в месяц
- ⚠️ Засыпает после 15 минут бездействия

**С Uptime Robot:**
- ✅ $0/месяц (бесплатный план)
- ✅ 50 мониторов
- ✅ Проверка каждые 5 минут

**Итого: $0/месяц** 🎉

---

## ✅ Чеклист перед деплоем

- [ ] Регистрация на Render.com
- [ ] Проект загружен в GitHub
- [ ] GitHub подключен к Render
- [ ] Web Service создан
- [ ] `TELEGRAM_BOT_TOKEN` добавлен в переменные окружения
- [ ] Деплой успешно завершен
- [ ] Приложение работает (проверен `/api/ping`)
- [ ] Uptime Robot настроен
- [ ] Бот (`bot.py`) обновлен с новым URL

---

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ Протестируйте бота в Telegram
2. ✅ Проверьте работу бронирования
3. ✅ Убедитесь, что Uptime Robot работает
4. ✅ Настройте резервное копирование БД (опционально)

---

**Готово! Ваш бот теперь работает в облаке! 🚀**

*Если возникнут вопросы, проверьте логи в Render Dashboard или обратитесь за помощью.*

