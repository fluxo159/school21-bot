# 🚀 Быстрый деплой на Railway.app

## Пошаговая инструкция (5 минут)

### 1. Подготовка кода
```bash
# Убедитесь, что код загружен в GitHub
git add .
git commit -m "Prepare for deployment"
git push
```

### 2. Создание проекта на Railway

1. Перейдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий
5. Railway автоматически определит Python проект

### 3. Настройка переменных окружения

В настройках проекта (Settings → Variables) добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
FLASK_DEBUG=False
```

**Важно**: `BACKEND_URL` добавите после получения URL проекта!

### 4. Получение URL проекта

1. После деплоя Railway даст вам URL (например: `https://your-project.railway.app`)
2. Добавьте переменную окружения:
   ```
   BACKEND_URL=https://your-project.railway.app
   ```
3. Перезапустите сервис (Settings → Restart)

### 5. Запуск Telegram бота (второй сервис)

1. В том же проекте нажмите **"+ New"** → **"Empty Service"**
2. В настройках сервиса:
   - **Start Command**: `python bot.py`
   - Добавьте те же переменные окружения
3. Сохраните и дождитесь запуска

### 6. Проверка

1. Откройте Telegram бота
2. Нажмите `/start`
3. Откройте мини-приложение
4. Проверьте работу

## ⚡ Альтернатива: Render.com

Если Railway не подходит, используйте Render:

1. https://render.com → Sign Up
2. New → Web Service
3. Connect GitHub repo
4. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `cd backend && gunicorn app:app --bind 0.0.0.0:$PORT`
   - **Environment**: Python 3
5. Добавьте переменные окружения
6. Deploy!

## 💡 Полезные команды

**Просмотр логов:**
- Railway: Dashboard → Service → Logs
- Render: Dashboard → Service → Logs

**Перезапуск:**
- Railway: Settings → Restart
- Render: Manual Deploy → Clear build cache & deploy

## ⚠️ Важно для тестирования

1. **База данных**: SQLite будет работать, но данные могут потеряться при перезапуске
2. **Бесплатный tier**: Railway дает $5 кредитов в месяц (достаточно для теста)
3. **Домен**: Railway автоматически дает домен вида `your-project.railway.app`

## 🆘 Проблемы?

**Приложение не запускается:**
- Проверьте логи в Dashboard
- Убедитесь, что `PORT` берется из переменных окружения
- Проверьте, что все зависимости в `requirements.txt`

**Telegram bot не работает:**
- Убедитесь, что bot сервис запущен отдельно
- Проверьте `TELEGRAM_BOT_TOKEN`
- Проверьте `BACKEND_URL` (должен быть полный URL с https://)

