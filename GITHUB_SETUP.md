# 📦 Инструкция по загрузке кода в GitHub

## Шаг 1: Создание репозитория на GitHub

1. **Перейдите на https://github.com**
2. **Войдите** в свой аккаунт (или создайте новый, если нет)
3. **Нажмите кнопку "+"** в правом верхнем углу → **"New repository"**
4. **Заполните форму:**
   - Repository name: `school21-bot` (или любое другое название)
   - Description: `Telegram bot for room booking`
   - Выберите **Public** (для бесплатного использования)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
5. **Нажмите "Create repository"**

## Шаг 2: Загрузка кода в репозиторий

После создания репозитория GitHub покажет инструкции. Выполните следующие команды в терминале:

### Вариант A: Если вы уже в папке проекта

```bash
# 1. Добавьте все файлы
git add .

# 2. Создайте первый коммит
git commit -m "Initial commit: School 21 room booking bot"

# 3. Переименуйте ветку в main (если нужно)
git branch -M main

# 4. Подключите удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/school21-bot.git

# 5. Загрузите код
git push -u origin main
```

### Вариант B: Если нужно скопировать команды с GitHub

GitHub покажет вам команды после создания репозитория. Они будут выглядеть так:

```bash
git remote add origin https://github.com/YOUR_USERNAME/school21-bot.git
git branch -M main
git push -u origin main
```

Но сначала нужно сделать:
```bash
git add .
git commit -m "Initial commit"
```

## Шаг 3: Проверка

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Вы должны увидеть все ваши файлы в репозитории

## ⚠️ Важно

- **Не загружайте файл `.env`** - он уже в `.gitignore`
- **Не загружайте базу данных** - файлы `.db` тоже в `.gitignore`
- Если GitHub попросит логин/пароль, используйте **Personal Access Token** вместо пароля

## 🔑 Создание Personal Access Token (если нужно)

Если Git просит пароль:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права доступа (нужен `repo`)
4. Скопируйте токен
5. Используйте токен вместо пароля при `git push`

## ✅ После загрузки

Теперь можно переходить к деплою на Railway:
1. Откройте https://railway.app
2. New Project → Deploy from GitHub repo
3. Выберите ваш репозиторий `school21-bot`

