# 🚀 Инструкция по деплою на Railway.app

## Бесплатный хостинг для тестирования

### Шаг 1: Подготовка

1. **Создайте аккаунт на Railway.app**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Подготовьте репозиторий GitHub**
   - Загрузите код в GitHub репозиторий
   - Убедитесь, что все файлы загружены

### Шаг 2: Создание проекта на Railway

1. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

2. **Настройте переменные окружения**
   - Перейдите в Settings → Variables
   - Добавьте следующие переменные:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен_бота
     BACKEND_URL=https://ваш-проект.railway.app
     FLASK_DEBUG=False
     PORT=5000
     ```

### Шаг 3: Настройка сервисов

Railway автоматически определит Python проект. Если нужно:

1. **Backend сервис (Flask)**
   - Railway автоматически запустит `backend/app.py`
   - Убедитесь, что порт берется из переменной окружения `PORT`

2. **Bot сервис (Telegram Bot)**
   - Создайте второй сервис в том же проекте
   - Выберите "Empty Service"
   - В настройках укажите команду запуска: `python bot.py`
   - Добавьте те же переменные окружения

### Шаг 4: Настройка базы данных

1. **SQLite (простой вариант)**
   - SQLite файл будет храниться в файловой системе
   - ⚠️ При перезапуске данные могут потеряться (для теста подойдет)

2. **PostgreSQL (рекомендуется для продакшена)**
   - Добавьте PostgreSQL сервис в Railway
   - Обновите `database.py` для использования PostgreSQL

### Шаг 5: Настройка Telegram Webhook

После деплоя получите URL вашего приложения (например: `https://your-project.railway.app`)

1. **Обновите BACKEND_URL** в переменных окружения
2. **Перезапустите сервисы**

### Шаг 6: Проверка

1. Откройте ваш Telegram бот
2. Нажмите /start
3. Откройте мини-приложение
4. Проверьте, что все работает

## 🔧 Альтернативные варианты

### Render.com (тоже бесплатный)

1. Создайте аккаунт на https://render.com
2. Создайте Web Service
3. Подключите GitHub репозиторий
4. Настройте:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `cd backend && python app.py`
5. Добавьте переменные окружения

### PythonAnywhere (бесплатный, но ограниченный)

1. Создайте аккаунт на https://www.pythonanywhere.com
2. Загрузите код через Git или вручную
3. Настройте Web App
4. Ограничение: бесплатный аккаунт просыпается только при запросах

## ⚠️ Важные замечания

1. **База данных**: SQLite может терять данные при перезапуске. Для продакшена используйте PostgreSQL
2. **Переменные окружения**: Никогда не коммитьте `.env` файл в Git
3. **Порты**: Railway автоматически назначает порт через переменную `PORT`
4. **Логи**: Проверяйте логи в Railway Dashboard для отладки

## 📝 Чеклист перед деплоем

- [ ] Код загружен в GitHub
- [ ] `requirements.txt` актуален
- [ ] Переменные окружения настроены
- [ ] `BACKEND_URL` указывает на правильный домен
- [ ] Telegram bot token получен
- [ ] База данных инициализирована

## 🆘 Решение проблем

**Проблема**: Приложение не запускается
- Проверьте логи в Railway Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте переменные окружения

**Проблема**: База данных не работает
- Проверьте права доступа к файлу БД
- Убедитесь, что `init_db()` вызывается при запуске

**Проблема**: Telegram bot не отвечает
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Убедитесь, что bot сервис запущен
- Проверьте логи bot сервиса

